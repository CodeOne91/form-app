import Form from "../containers/Form";
import { FlexContainer } from "../styles/wrapper";

const HomePage = () => {
  return (
    <FlexContainer size={100}>
      <Form></Form>
    </FlexContainer>
  );
};

export default HomePage;
